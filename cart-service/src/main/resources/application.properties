spring.application.name=cart-service

spring.data.neo4j.username=${cart-db-user}
spring.data.neo4j.password=${cart-db-password}
spring.data.neo4j.uri=bolt://${cart-db-host}

product.service.client.uri=${product-service-host}:${product-service-port}/product

server.servlet.context-path=/cart
